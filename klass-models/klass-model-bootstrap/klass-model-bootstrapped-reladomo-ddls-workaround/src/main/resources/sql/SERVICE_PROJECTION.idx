alter table SERVICE_PROJECTION add constraint SERVICE_PROJECTION_PK primary key (id);

drop index if exists SERVICE_PROJECTION_IDX1;
create index SERVICE_PROJECTION_IDX1 on SERVICE_PROJECTION(class_name);

