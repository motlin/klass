alter table SERVICE add constraint SERVICE_fk_0 foreign key (
    projection_name
)
references SERVICE_PROJECTION(
    name
);

alter table SERVICE add constraint SERVICE_fk_1 foreign key (
    class_name,url_string
)
references URL(
    class_name,url
);

