alter table PROJECTION_REFERENCE_PROPERTY add constraint PROJECTION_REFERENCE_PROPERTY_FK_PARENT foreign key (
    parent_id
)
references PROJECTION_ELEMENT(
    id
);

alter table PROJECTION_REFERENCE_PROPERTY add constraint PROJECTION_REFERENCE_PROPERTY_FK_ASSOCIATION_END foreign key (
    association_end_class, association_end_name
)
references ASSOCIATION_END(
    owning_class_name, name
);

