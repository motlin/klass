alter table AND_CRITERIA add constraint AND_CRITERIA_FK_LEFT foreign key (
    left_id
)
references CRITERIA(
    id
);

alter table AND_CRITERIA add constraint AND_CRITERIA_FK_RIGHT foreign key (
    right_id
)
references CRITERIA(
    id
);

