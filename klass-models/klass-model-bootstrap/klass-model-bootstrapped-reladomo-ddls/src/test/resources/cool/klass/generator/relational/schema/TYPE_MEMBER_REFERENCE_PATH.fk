alter table TYPE_MEMBER_REFERENCE_PATH add constraint TYPE_MEMBER_REFERENCE_PATH_FK_KLASS foreign key (
    class_name
)
references KLASS(
    name
);

alter table TYPE_MEMBER_REFERENCE_PATH add constraint TYPE_MEMBER_REFERENCE_PATH_FK_DATA_TYPE_PROPERTY foreign key (
    property_class_name, property_name
)
references DATA_TYPE_PROPERTY(
    classifier_name, name
);

