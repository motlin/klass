alter table OR_CRITERIA add constraint OR_CRITERIA_FK_LEFT foreign key (
    left_id
)
references CRITERIA(
    id
);

alter table OR_CRITERIA add constraint OR_CRITERIA_FK_RIGHT foreign key (
    right_id
)
references CRITERIA(
    id
);

