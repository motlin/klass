alter table BINARY_CRITERIA add constraint BINARY_CRITERIA_FK_LEFT foreign key (
    left_id
)
references CRITERIA(
    id
);

alter table BINARY_CRITERIA add constraint BINARY_CRITERIA_FK_RIGHT foreign key (
    right_id
)
references CRITERIA(
    id
);

