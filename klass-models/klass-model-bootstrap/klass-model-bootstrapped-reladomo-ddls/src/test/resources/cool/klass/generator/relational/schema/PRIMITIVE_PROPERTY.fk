alter table PRIMITIVE_PROPERTY add constraint PRIMITIVE_PROPERTY_FK_OWNING_CLASSIFIER foreign key (
    classifier_name
)
references CLASSIFIER(
    name
);

alter table PRIMITIVE_PROPERTY add constraint PRIMITIVE_PROPERTY_FK_OWNING_CLASSIFIER foreign key (
    classifier_name
)
references CLASSIFIER(
    name
);

alter table PRIMITIVE_PROPERTY add constraint PRIMITIVE_PROPERTY_FK_OWNING_CLASSIFIER foreign key (
    classifier_name
)
references CLASSIFIER(
    name
);

