alter table URL_PARAMETER add constraint URL_PARAMETER_FK_URL foreign key (
    service_group_name, url_string
)
references URL(
    service_group_name, url
);

alter table URL_PARAMETER add constraint URL_PARAMETER_FK_PARAMETER foreign key (
    parameter_id
)
references PARAMETER(
    id
);

