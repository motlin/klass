alter table SERVICE_ORDER_BY add constraint SERVICE_ORDER_BY_FK_SERVICE foreign key (
    service_group_name, service_url_string, service_verb
)
references SERVICE(
    service_group_name, url_string, verb
);

alter table SERVICE_ORDER_BY add constraint SERVICE_ORDER_BY_FK_THIS_MEMBER_REFERENCE_PATH foreign key (
    this_member_reference_path_id
)
references THIS_MEMBER_REFERENCE_PATH(
    id
);

