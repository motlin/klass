<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project
        xmlns="http://maven.apache.org/POM/4.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>xample-projects</artifactId>
        <groupId>cool.klass</groupId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>
    <properties>
        <klass.version>0.1.0-SNAPSHOT</klass.version>
        <app.artifactId>reladomo-to-many-abstract</app.artifactId>
        <app.rootPackageName>com.repro.reladomo.tomanyabstract</app.rootPackageName>
        <app.name>ReladomoToManyAbstract</app.name>
        <mainClass>${app.rootPackageName}.dropwizard.application.${app.name}Application</mainClass>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <maven.compiler.parameters>true</maven.compiler.parameters>
    </properties>
    <groupId>com.repro.reladomo.tomanyabstract</groupId>
    <artifactId>reladomo-to-many-abstract</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>${app.name} (Module Group)</name>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.liftwizard</groupId>
                <artifactId>liftwizard-dependencies</artifactId>
                <version>0.1.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.liftwizard</groupId>
                <artifactId>liftwizard-bom</artifactId>
                <version>${liftwizard.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <build>
        <defaultGoal>clean install</defaultGoal>
        <plugins>
            <plugin>
                <artifactId>maven-checkstyle-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-enforcer-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>versions-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <modules>
        <module>reladomo-to-many-abstract-domain-model</module>
        <module>reladomo-to-many-abstract-reladomo-definitions</module>
        <module>reladomo-to-many-abstract-reladomo-pojos</module>
        <module>reladomo-to-many-abstract-reladomo-ddls</module>
        <module>reladomo-to-many-abstract-services</module>
        <module>reladomo-to-many-abstract-dtos</module>
        <module>reladomo-to-many-abstract-json-views</module>
        <module>reladomo-to-many-abstract-dropwizard-application</module>
        <module>reladomo-to-many-abstract-meta-model-constants</module>
    </modules>
</project>
